# AI-Invoice-Processing-Engine
AI-powered invoice processing workflow that extracts data from PDF invoices, validates suppliers, prevents duplicates, and automatically creates or updates invoice records using n8n.
